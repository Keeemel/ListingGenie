# Gemini API key (free tier) — https://aistudio.google.com/app/apikey
# Without this key, the on-page audit (score + issues) still works fully.
# Keyword gap analysis requires the key.
GEMINI_API_KEY=your_gemini_api_key_here
